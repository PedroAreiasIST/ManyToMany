## .NET build output (generated by the build; should never be committed)
[Bb]in/
[Oo]bj/

## NuGet restore output
project.lock.json
*.nupkg
*.snupkg

## Editor backup / swap / temporary files
*~
*.swp
*.tmp
*.bak

## OS-generated files
.DS_Store
Thumbs.db

## Nested independent git repos checked out inside this working tree
## (intentionally kept independent -- never embed them as submodules/gitlinks)
/SimPlas/
/ISTVectorPostprocessor/

## Large local backup tarball (over GitHub's 100 MB limit -- keep on disk, out of git)
/SimPlas.tar.gz

## Benchmark local environments & generated artifacts (not for the repo)
benchmarks/.venv/
benchmarks/.venv39/
benchmarks/python-*-amd64.exe
benchmarks/results*.csv
benchmarks/results*.md
benchmarks/results*.tex

## BenchmarkDotNet run output
BenchmarkDotNet.Artifacts/
